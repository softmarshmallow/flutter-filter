import 'package:flutter/material.dart';


/// one is selectable in multiple options
class FilterChoice extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
